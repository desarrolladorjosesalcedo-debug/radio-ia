# requirements.txt
# Dependencias Python del proyecto Radio IA
# Instalar con: pip install -r requirements.txt

# Manejo de configuración YAML
pyyaml>=6.0.1

# API de Groq para LLM rápido
groq>=0.4.0

# Utilidades de sistema
psutil>=5.9.0

# Logging mejorado (opcional)
colorlog>=6.7.0

# Text-to-Speech (TTS)
gtts>=2.5.0              # Google TTS (fallback)
edge-tts>=7.0.0          # Microsoft Edge TTS (recomendado - voces neuronales)
elevenlabs>=1.0.0        # ElevenLabs TTS (voces ultrarrealistas - requiere API key)

# Servidor Web API (para interfaz web)
fastapi>=0.104.0         # Framework API REST moderno
uvicorn[standard]>=0.24.0 # Servidor ASGI para FastAPI
python-multipart>=0.0.6  # Soporte para formularios
